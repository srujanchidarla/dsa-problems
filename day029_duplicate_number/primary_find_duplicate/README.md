---
dayNumber: 29
title: "Duplicate Number in Array"
difficulty: "Medium"
pattern: "HashSet Tracking"
approach: "Traverse the array and store each element in a set. If an element is already in the set, it is the duplicate."
---

# Find Duplicate

**Logic:** Using a HashSet provides $O(1)$ average time for lookups, making the whole process $O(N)$.
